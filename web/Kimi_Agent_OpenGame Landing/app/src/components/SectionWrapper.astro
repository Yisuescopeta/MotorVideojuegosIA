---
interface Props {
  id?: string;
  className?: string;
  style?: string;
}

const { id, className = '', style } = Astro.props;
---

<section {id} class={`section-animate ${className}`} style={style}>
  <slot />
</section>